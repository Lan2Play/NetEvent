@page "/administration/users"
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using NetEvent.Shared.Dto

@inject Microsoft.Extensions.Localization.IStringLocalizer<App> Localize


@attribute [Authorize(Roles = "Admin")]

<MudTabs Elevation="1" Rounded="true">
    <MudTabPanel Text="User">
        <MudDataGrid T="User"
                     MultiSelection="false"
                     Items="@AllUsers"
                     Sortable="true"
                     Filterable="true"
                     ReadOnly="false"
                     EditMode="DataGridEditMode.Form"
                     EditTrigger="DataGridEditTrigger.Manual"
                     CommittedItemChanges="@CommittedUserChanges"
                     QuickFilter="@_usersQuickFilter"
                     Hideable="true">
            <ToolBarContent>
                <MudText Typo="Typo.h6">@Localize["Administration.Users.Periodic"]</MudText>
                <MudSpacer />
                <MudTextField @bind-Value="_usersSearchString" Placeholder="Search" Adornment="Adornment.Start" Immediate="true"
                              AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
            </ToolBarContent>
            <Columns>
                @*<SelectColumn T="ApplicationUser" />*@
                <Column T="User" Field="UserName" Title="@Localize["Administration.Users.UserName"]" />
                <Column T="User" Field="FirstName" Title="@Localize["Administration.Users.FirstName"]" />
                <Column T="User" Field="LastName" Title="@Localize["Administration.Users.LastName"]" />
                <Column T="User" Field="Email" Title="@Localize["Administration.Users.Email"]" />
                @*<Column T="User" Field="PhoneNumber" Title="@Localize["Administration.Users.PhoneNumber"]" />*@
                <Column T="User" Field="EmailConfirmed">
                    <CellTemplate>
                        <MudCheckBox Checked="@context.Item.EmailConfirmed" ReadOnly="true" />
                    </CellTemplate>
                    <EditTemplate>
                        <MudCheckBox @bind-Checked="@context.Item.EmailConfirmed" Label="@Localize["Administration.Users.EmailConfirmed"]" />
                    </EditTemplate>
                </Column>
                <Column T="User" CellClass="d-flex justify-end">
                    <CellTemplate>
                        <MudIconButton Size="@Size.Small" Icon="@Icons.Outlined.Edit" OnClick="@context.Actions.StartEditingItem" />
                    </CellTemplate>
                </Column>
            </Columns>
            <PagerContent>
                <MudDataGridPager T="User" />
            </PagerContent>
        </MudDataGrid>
    </MudTabPanel>
</MudTabs>
