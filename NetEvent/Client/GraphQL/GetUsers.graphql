query GetUsers {
  users {
    ...User
  }
}

query GetUserById($userId: String!)
{
  user(id: $userId)
  {
    ...User
  }  
}

fragment User on ApplicationUser
{
  id,
  firstName,
  lastName,
  userName
}
